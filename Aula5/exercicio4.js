// 4. Chame a função `processaParametros` com um argumento `7` e atribua seu valor de retorno à variável `resultado`;


let resultado = 0;

function processaParametros(numero = 7){
    return resultado = (numero + 3) / 5;
}

console.log(processaParametros(resultado));

console.log(processaParametros());