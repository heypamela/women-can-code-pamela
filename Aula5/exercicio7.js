//7. Adicione os números de 5 a 0 (inclusive) em ordem decrescente para `meuArray` usar um `while`loop e imprima-o no final.

const meuArray = [5, 4, 3, 2, 1, 0];

let valor = 5;
while(valor >= 5){
    console.log(meuArray);
    valor --;
}